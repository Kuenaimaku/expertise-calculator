// import expertiseDefaults from "@/expertise.json"
export default {
    toQueryParams(expertiseList){
        expertiseList.forEach(function(value, key) {
            console.log(key, value)
        })
    }
}